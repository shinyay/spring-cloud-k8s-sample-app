spring:
  application:
    name: employee
  cloud:
    kubernetes:
      config:
        name: employee
        namespace: default
        enable-api: true
      secrets:
        name: employee
        namespace: default
        enable-api: true